## مشکل

اعتباری که قبلاً گفتم (`@Modir` / `Jelve14041404`) در دیتابیس وجود ندارد. تنها حساب ادمین فعلی با نام کاربری فارسی `مدیر` ثبت شده و رمز آن hash شده (قابل بازیابی نیست). به همین دلیل edge function پاسخ «نام کاربری یا رمز عبور اشتباه است» برمی‌گرداند.

## راه‌حل

با یک migration یک حساب ادمین جدید با نام کاربری `Modir` و رمز `Jelve14041404` ایجاد می‌کنم:

1. درج رکورد در `custom_users` با `username = 'Modir'`, `full_name = 'مدیر'` و `password_hash = 'Jelve14041404'` (به صورت plaintext — edge function در اولین login آن را به bcrypt تبدیل می‌کند، طبق منطق موجود در `auth-login/index.ts`).
2. درج نقش `admin` در `user_roles` برای این کاربر.
3. حساب فارسی `مدیر` دست‌نخورده باقی می‌ماند.

## بعد از اجرا

با `Modir` / `Jelve14041404` در صفحه `/login` وارد می‌شوید و نقش «مدیر» را انتخاب می‌کنید.

هیچ تغییر کد frontend یا edge function لازم نیست.